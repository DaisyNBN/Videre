# Kubernetes Ingress for Videre Backend
# Generated by Containerization Assist using AI

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: videre-backend
  namespace: videre
  labels:
    app: videre-backend
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.videre.local
      secretName: videre-backend-tls
  rules:
    - host: api.videre.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: videre-backend
                port:
                  number: 80
