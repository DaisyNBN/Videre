# Kubernetes Service for Videre Backend
# Generated by Containerization Assist using AI

apiVersion: v1
kind: Service
metadata:
  name: videre-backend
  namespace: videre
  labels:
    app: videre-backend
spec:
  type: ClusterIP
  selector:
    app: videre-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
