# Network Policy for Videre Backend
# Generated by Containerization Assist using AI

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: videre-backend-network-policy
  namespace: videre
  labels:
    app: videre-backend
spec:
  podSelector:
    matchLabels:
      app: videre-backend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: default
        - podSelector:
            matchLabels:
              app: videre-backend
      ports:
        - protocol: TCP
          port: 3000
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 3000
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 5432
    - to:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 3000
